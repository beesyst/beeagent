app:
  name: "BeeAgent"
  env: "dev"

run:
  mode: "telegram"

telegram:
  enabled: true
  bot_token_env: "TELEGRAM_BOT_TOKEN"
  chat_id_env: "CHAT_ID"
  telemetry_enabled: true

logging:
  clear_logs: true
  utc: true
  level: "INFO"

mock:
  seed: 42
  weeks: 4
  stores: 3
  skus: 12
  category: "Vitamins"

approval:
  reject_reason: "Rejected by operator"
