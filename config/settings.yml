app:
  name: "BeeAgent"
  env: "dev"

run:
  mode: "telegram"

telegram:
  enabled: true
  bot_token_env: "TELEGRAM_BOT_TOKEN"
  chat_id_env: "CHAT_ID"
  telemetry_enabled: true

logging:
  clear_logs: true
  utc: true
  level: "INFO"
